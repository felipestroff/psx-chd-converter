Optional: place `psxtract.exe` here to enable `.pbp` to `.chd` conversion.

Expected structure:
- tools/psxtract/psxtract.exe

The app automatically uses this binary during PBP preprocessing.

You can also download automatically with:
- scripts/fetch-psxtract.ps1
- scripts/build.ps1 -FetchMame
